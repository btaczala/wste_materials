#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>
#include <vector>

namespace utils {
std::vector<std::string> split(const std::string& buffer,
                               const std::string& separator = " ");
}

#endif /* UTILS_HPP */
